# Local development variables for docker-compose only.
# Copy to .env and replace values as needed for your machine.
POSTGRES_DB=directcash
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me-local
POSTGRES_PORT=5433
REDIS_PORT=6380
REDIS_PASSWORD=change-me-local
